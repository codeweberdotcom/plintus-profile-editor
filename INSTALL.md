# Инструкция по установке и использованию

## Установка

1. Убедитесь, что плагин находится в директории `wp-content/plugins/plintus-profile-editor/`

2. Установите зависимости:
```bash
cd wp-content/plugins/plintus-profile-editor
npm install
```

3. Соберите React приложение:
```bash
npm run build
```

4. Активируйте плагин в WordPress:
   - Перейдите в `Плагины` → `Установленные плагины`
   - Найдите "Plintus Profile Editor"
   - Нажмите "Активировать"

## Использование

1. После активации в админ-панели появится меню **"Plintus Profiles"**

2. Создайте новый профиль:
   - Нажмите "Добавить новый"
   - Введите название профиля
   - Редактор загрузится автоматически

3. Инструменты редактора:
   - **Select** (👆) - выбор элементов для редактирования
   - **Line** (📏) - рисование линий под углом 90°
     - Кликните первую точку, затем вторую
     - Линия автоматически выровняется под 90°
   - **Arc** (◯) - создание скруглений (дуг)
     - Кликните по канвасу для размещения дуги
     - Настройте радиус и углы в панели свойств
   - **Delete** (🗑️) - удаление элементов
     - Выберите элемент и нажмите Delete или используйте инструмент

4. Настройки сетки:
   - **Step** - размер шага сетки (по умолчанию 10px)
   - **Snap** - включить/выключить привязку к сетке
   - **Grid** - показать/скрыть сетку

5. Редактирование элементов:
   - Выберите элемент инструментом Select
   - В правой панели появятся свойства элемента
   - Измените координаты, длины, радиусы и т.д.

## Режим разработки

Для разработки с автоматической пересборкой:
```bash
npm run dev
```

Для запуска dev-сервера (опционально):
```bash
npm start
```

## Структура данных

Профиль сохраняется в мета-поле `_profile_data` и содержит:
```json
{
  "elements": [
    {
      "id": "element-123",
      "type": "line",
      "start": { "x": 0, "y": 0 },
      "end": { "x": 100, "y": 0 },
      "length": 100
    },
    {
      "id": "element-456",
      "type": "arc",
      "center": { "x": 100, "y": 0 },
      "radius": 20,
      "startAngle": 0,
      "endAngle": 90
    }
  ],
  "grid": {
    "step": 10,
    "snap": true,
    "visible": true
  },
  "viewbox": {
    "x": 0,
    "y": 0,
    "width": 800,
    "height": 400
  }
}
```

## Troubleshooting

### Редактор не загружается
- Убедитесь, что выполнили `npm run build`
- Проверьте консоль браузера на наличие ошибок
- Убедитесь, что React и ReactDOM загружаются из CDN (проверьте класс Admin.php)

### Ошибки при сохранении
- Проверьте права доступа пользователя (должны быть права на редактирование постов)
- Проверьте REST API в WordPress

### Сетка не отображается
- Нажмите кнопку "Grid" в панели инструментов
- Проверьте, что `grid.visible` установлен в `true`

